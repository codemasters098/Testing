<div class="hero">
  <p class="large brandon">INSPIRING PROPERTY<br>IS COMING</p>
  <p class="small brandon">BRIDGING THE KNOWLEDGE GAP BETWEEN<br>INVESTORS AND FIRST HOME BUYERS<br>SINCE LIKE...RIGHT NOW.</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>